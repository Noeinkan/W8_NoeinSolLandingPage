# Agent Instructions

**The instructions for this repository live in [`CLAUDE.md`](./CLAUDE.md). Read that file.**

It is the single source of truth for the file tree, CSS/JS conventions, the bilingual EN/IT workflow, deployment, and the test suite.

This file exists only so that agents following the `AGENTS.md` convention find their way there. Do not duplicate content into it — a previous full copy silently drifted a whole visual redesign out of date.
