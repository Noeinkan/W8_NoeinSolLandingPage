# Local planning notes — not for the repo or deploy
*.local.md

# Internal-only documents — never for the deployed site or public repo
PRICING.md

# Python virtual environment
.venv/

# Test dependencies (smoke test installs jsdom into scripts/tests/smoke/node_modules on first run)
scripts/tests/smoke/node_modules/
scripts/tests/smoke/package-lock.json
scripts/tests/smoke/package.json

# OG card generator dependencies (installs playwright into scripts/og/node_modules on first run)
scripts/og/node_modules/
scripts/og/package-lock.json
scripts/og/package.json

# Eleventy build output and dependencies
_site/
node_modules/
