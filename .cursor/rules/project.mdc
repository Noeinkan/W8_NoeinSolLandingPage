---
description: Pointer to CLAUDE.md, the single source of truth for this repository
alwaysApply: true
---

# Noein Solutions — Landing Page

**The instructions for this repository live in [`CLAUDE.md`](../../CLAUDE.md). Read that file.**

It is the single source of truth for the file tree, the design system, CSS/JS conventions, the
bilingual EN/IT workflow, deployment, and the test suite.

This file exists only so that Cursor finds its way there. Do not duplicate content into it. The
version this replaced described plain HTML pages at the repo root, a single ~2,400-line
stylesheet, "no build step, no package.json", and Instrument Serif — every one of those facts had
been false for months, and `alwaysApply: true` meant it was injected into every session.
